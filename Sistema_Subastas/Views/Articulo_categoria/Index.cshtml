@model IEnumerable<Sistema_Subastas.Models.articulo_categoria>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.categoria_id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.categoria_id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.articulo_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.articulo_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.articulo_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
